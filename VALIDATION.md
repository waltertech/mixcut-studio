# 第一版验收记录

## 环境与范围

- 本地 Mac，Python 3.13.6；已有 FFmpeg / ffprobe。
- 服务地址：`http://127.0.0.1:8877`，仅监听本机。
- 实际素材：4 段 TS 录屏、9 首 MP3；原始素材未改写。
- 不依赖模型 API，不接入 Jev。

## 自动测试

命令：`python3 -m unittest discover -s tests -v`

此前目录选择、关键帧、审核归档与贴图模板更新共63项测试通过；音乐命名更新另加6项，结果见下文。

- 完整音乐排列、固定首曲的组合上限、歌曲选择与时长边界。
- 有限搜索、明确无匹配与达到搜索上限的不同提示。
- 单源起点间隔、多源不重叠区间、显式分组、总数量上限。
- SQLite 持久化、失败重试上限、暂停/停止、素材变更、输出已落盘但未提交状态的恢复。
- 已完成输出被删除/修改后的恢复检查。
- HTTP 音视频范围读取、Host/Origin 限制及文件访问白名单。
- FFmpeg 真实合成测试：有声/静音视频混合、非零起点、整首音频拼接、解码时长缓存、进度回调、禁止覆盖文件。

测试音频使用临时合成纯音，不能替代对实际歌曲的主观试听。

## 真实素材样片

| 模式 | 批次 | 配置 | 结果 |
|---|---|---|---|
| 单源连续截取 | `7cedb0bb15a3` | 1280×720 / 30fps；歌曲07＋歌曲03；原音静音 | 完成；计划46.36秒；约20.45MB；端到端约6秒 |
| 多源拼接 | `f00aa09d275e` | 1280×720 / 30fps；歌曲07＋歌曲03；原音15% | 完成；计划46.36秒；约20.54MB；端到端约4秒 |

文件位于 `exports/验收样片/对应批次/001.mp4`。

两条均通过音视频流时长检查与 FFmpeg 全片解码检查，并抽样查看了画面。不是逐帧人工验收，也未宣称已人工试听。

开发中曾发现 TS 原始容器在靠近尾部的定位产生不足约0.8秒的画面，时长校验拦截了输出。已修复为先生成无损转封装缓存再精确截取，完整音乐没有被缩短。原失败批次 `1b732977a526` 经网页点击重试后已成功完成。

## 50 条批量验收

- 批次：`5674fd9188ce`。
- 模式：多源拼接；4首候选歌曲为歌曲03、07、06、09，每条1～4首，各首完整使用。
- 规格：640×360 / 24fps，专门用于批量执行验证；正常界面默认720p。
- 50条计划总时长：16616.06秒，约4小时36分56秒。
- 输出目录：`exports/批量验收360p/5674fd9188ce/`。
- 结果：50/50成功，0失败；每条均通过音视频时长检查与全片解码校验。
- 视频方案指纹50个不同值，音乐有序列表指纹50个不同值。
- 本轮端到端耗时1285.65秒（约21分26秒），文件共2081141178字节（约2.08GB / 1.94GiB）。
- 全部采用本机 `h264_videotoolbox` 编码；运行期间也进行了界面和其他测试，数据仅代表本次实测。

720p样片与360p批次是不同规格的测试，不能把360p性能当作720p/1080p性能承诺。

## 网页与恢复

- 已在真实浏览器走通素材扫描、参数设置、生成方案、开始任务、排队暂停、失败项重试。
- 已验证网页刷新后能重新读取素材和批次。
- 三条720p批次 `d8e795991cae` 用于网页启动/暂停与后台重启续跑验收，已完成3/3。
- 实机流程：先完成2条后暂停 → 退出后台 → 运行实际启动器 `python3 launch.py` → 恢复剩余1条 → 3/3完成。
- 原2条成片的文件大小与纳秒级修改时间保持一致，没有重新导出。证据保存于 `.mixcut/recovery-check.json`。
- 最终全部5个验收批次状态均为completed，共56条成片；33项自动化测试通过。
- 最终13个输入素材的内容指纹全部匹配；50条批次的音乐流时长与计划在报告精度内一致。
- 启动器已实测，后台作为独立进程保留运行，网页可直接操作。

## 明确限制

- 当前是可双击启动的本地源码工具，未封装为独立安装包；其他电脑需要先安装 Python 和 FFmpeg。
- Mac原生文件夹选择器已于目录选择更新中加入，同时保留绝对路径输入。
- 同批视频方案和音乐顺序分别去重，但画面可重叠；不提供平台原创判定或视觉语义去重。
- 单条最多128个视频片段，每批最多500条；过大音乐池使用有限搜索，可能无法遍历所有组合。
- 组内拼接需要至少两个视频填写同一个非空组名。
- 暂停/停止在当前条完成后生效；程序中断后重新处理未完成条目。
- 完整音乐、技术可播放性已经检查，歌曲接缝听感、精彩程度和原音比例仍需用户试听决定。

## 目录选择更新（2026-09-22）

- 视频、音乐、导出目录均新增系统文件夹选择按钮，取消保留原路径。
- 导出目录偏好持久化；新批次使用本地日期与流水号子目录。
- 并发预留、跳过已有目录、重启递增、取消选择、参数传递与旧任务路径兼容均新增测试。
- 实际批次 `1c6b64fe63f4` 已完成2条，保存到 `exports/2026-09-22_001/`。

## 关键帧预览更新（2026-09-22）

- 为现有成片按需提取编码关键帧，时间戳和缩略图按文件路径、大小、修改时间缓存。
- 默认12张均匀概览覆盖全片，支持展开全部、悬停/聚焦大图和点击定位。
- 实测 `2026-09-22_001/001.mp4` 的200秒成片包含500张关键帧，首尾为0秒、199.6秒；网页展开后500个条目均存在。
- 新增6项测试：完整关键帧顺序、图片/元数据缓存、大/小图尺寸、越界/旧版本拒绝、变更文件缓存失效、只允许已完成任务访问。
- 浏览器实测：展开全部后500个条目；刷新状态仍保留500个条目及播放位置。首次点击00:36.400关键帧，播放器已加载并暂停定位在36.4秒，大图正常显示。

## 审核归档更新（2026-09-22）

- 素材和任务结果页均提供审核目录设置；每条成功成片可点击“通过审核”。
- 采用完整复制、SHA-256副本校验、日期编号归档，保留原导出；通过状态与路径持久化。
- 新增9项临时文件测试：保留原件、同批归组、并发重复请求幂等、部分偏好保存、未完成拒绝、失败重试、同名不同文件不覆盖、归档落盘后状态中断恢复、原件修改拒绝及更换审核根目录。
- 测试没有代替用户审核真实成片，审核决定由用户点击按钮作出。
- 浏览器已确认任务页审核目录和58条现有成片的审核入口显示正常。联调中修复了重复写入按钮文字引发的DOM观察器循环，使用新的预览页验证通过。

## 贴图库与模板更新（2026-09-22）

- 新增5项真实FFmpeg贴图测试和4项应用层持久化测试；完整套件63项通过。
- 覆盖静态格式/尺寸检查、透明RGBA导入、位置/透明度/时段、生成覆盖层、模板快照、无模板路径、图片防篡改、原片/原审核不变与新版本排队。
- 隔离浏览器实测：上传测试贴图→拖动定位（坐标字段同步）→保存和更新同一模板→审核页套用→独立新版本完成→生成前选择同一模板→新成片完成。
- 测试模板为x=65%、y=65%、宽25%、不透明度50%、0.5～2秒显示；截图确认贴图出现在正确位置。编辑器默认不启用贴图。
- 审核版本与原视频音轨包的SHA-256均为 `8a5bcec3141f5d0211baa377dc7905d8de83229e9b03996d12876ce4f4d03420`，确认音轨直接复制。
- 浏览器测试使用独立临时目录和8891端口，没有给用户真实贴图库添加示例素材，也未替用户审核真实视频。

## 音乐风格命名更新（2026-09-22）

- 风格以文件夹名为依据；三个已知风格支持根目录直接选择、总目录递归扫描及专辑嵌套。
- 新成片按风格与批次内编号命名，混合风格使用组合标签；旧批次路径与文件名不修改。
- 新增6项测试覆盖目录识别、其他风格、组合标签、安全文件名、缓存扫描标签、新方案与审核保留名称。
- 完整回归测试69项通过，正式本地服务已重启启用新命名规则。

## 定时任务更新（2026-09-22）

- 在应用本地后台实现定时调度，不依赖Codex提醒或外部模型接口；正式环境未预设任何自动任务。
- 新增部分规划、跨检查去重、调度状态测试及真实FFmpeg取消测试。
- 真实媒体测试：先生成2/4，重复扫描不新增；新增视频后补足4/4；随后50条目标只生成4条，下一任务启动后旧目标被替代，新任务独立完成1/1，旧输出内容保持一致。
- 取消编码测试确认没有最终成片，也没有残留的部分导出文件。
- 隔离浏览器测试：保存任务、立即启动、完成1/50后等待、下一任务接管并完成1/1；历史记录保留旧任务1/50和取消原因。
- 一次/每天/每周使用电脑本地时间；后台关闭或休眠不执行，恢复时只启动最近的错过轮次。
- 完整自动测试83项通过。正式环境仍有0个定时任务，须由用户设置并启用。

## 源视频时长门槛（2026-09-22）

- “剪辑设置”新增分钟单位的源文件门槛，严格大于才参与；0不限制，支持小数。
- 新增5项测试覆盖单源/多源、正好等于边界、0与小数、全部排除的手动提示/定时等待，以及配置校验和定时快照持久化。
- 与部分规划和调度测试合计17项针对性检查通过。
