# 批量混剪工具 TODO

更新：2026-09-21  
依据：[项目规划](./PROJECT_PLAN.md)  
状态：第一版已交付，50条批量验收与实机重启续跑通过；用户听感确认与后续版本单独保留。

## 已完成的需求准备

- [x] 检查当前 4 段视频和 9 首音乐的时长、格式。
- [x] 抽样查看 4 段视频，确认混合规格及画面类型。
- [x] 确认单源连续截取、多源拼接两种模式。
- [x] 确认歌曲完整播放，允许改变选曲和顺序。
- [x] 确认第一版不接 Jev，采用本地网页形态。
- [x] 写出规划、范围、边界和阶段验收标准。

## M1：素材与方案规划

- [x] 建立最小项目结构，检查 Python、FFmpeg、ffprobe 与本机编码能力。
- [x] 建立素材、批次、成片方案、执行状态的数据存储（SQLite 保存批次快照与素材索引）。
- [x] 递归扫描指定目录，排除 ZIP、隐藏文件和输出目录。
- [x] 读取视频/音频规格；明确标记无法探测或解码的素材。
- [x] 为素材计算并缓存内容指纹，合并同内容不同文件名的输入。
- [x] 提供手动分组、视频缩略图、音乐试听所需的本地接口。
- [x] 实现固定歌曲集合的不同排列。
- [x] 实现候选歌曲池选子集，支持歌曲数量和总时长范围。
- [x] 支持指定首曲或首曲候选池，单条内不重复歌曲。
- [x] 实现组合不足、时长不匹配、搜索上限等不同提示。
- [x] 实现单源不重叠顺切与允许重叠的分散起点取段。
- [x] 实现多源区间分配，满足至少两个源文件及段长范围。
- [x] 实现视频计划去重、音乐有序列表去重和区间重合统计。
- [x] 保存随机种子和完整方案，使同一计划可复现。
- [x] 当前素材生成可行的 50 条计划。
- [x] 验证边界：素材短于音乐；固定 5 首且固定首曲仅 24 种；段长不可满足；计划搜索不足。

## M2：最小导出闭环

- [x] 验证 TS 视频定位、时戳处理与连续区间截取（先无损转封装为定位缓存）。
- [x] 实现画幅、帧率统一及默认不裁切的缩放策略。
- [x] 实现多段画面拼接并匹配完整音乐总时长。
- [x] 完整解码各首音乐后顺序接续，正确处理编码延迟/填充。
- [x] 保证不截歌、不变速、不交叉重叠、不自动裁静音。
- [x] 实现原音静音/保留、原音与音乐音量设置。
- [x] 导出到临时文件，检查音视频流、时长和可解码性后生成最终文件。
- [x] 输出每条对应的源片段与音乐清单。
- [x] 用当前素材制作两种模式各至少 1 条 720p 样片。
- [x] 抽样查看成片画面，技术校验完整音乐、音视频时长与解码。
- [ ] 用户试听确认歌曲接缝、原音比例及内容审美（不以技术校验代替听感确认）。
- [x] 实测并记录样片渲染速度、文件大小；据此估计批量成本。

## M3：批量可靠性

- [x] 实现持久化任务队列，默认单条串行处理。
- [x] 展示成功、待处理、运行中、失败、校验中状态。
- [x] 成功数达到目标才标记完成，失败不计数。
- [x] 实现有上限的自动重试与手动重试。
- [x] 实现当前条完成后暂停，以及继续、停止功能。
- [x] 后台重启后核对文件与任务状态，只恢复未完成条目（自动化与实机复核均通过）。
- [x] 识别素材变化，防止旧方案无提示地使用已替换素材。
- [x] 检查输出目录可写和空间，磁盘不足暂停并显示错误。
- [x] 防止重复点击或多个启动实例同时执行同一批次。
- [x] 验证编码成功但状态未提交等中断场景，避免重复计数。
- [x] 样片通过后，用可行参数完成一次 50 条批量导出（360p验收规格，50/50成功）。
- [x] 校验全部 50 条文件，并分别核对视频和音乐方案唯一性。

## M4：本地网页与启动入口

- [x] 素材页：视频、音乐、导出目录支持 Mac 原生文件夹选择及绝对路径输入；取消不改动原路径。
- [x] 新批次保存到所选导出目录下的日期编号子文件夹，自动递增、防止覆盖，记住导出目录，兼容旧任务。
- [x] 设置页：两种模式、数量、期望时长、段长、重叠规则、音乐规则。
- [x] 设置页：输出路径、分辨率、原音与音乐音量。
- [x] 方案页：实际时长、素材区间、歌曲顺序、重复统计、不可行原因。
- [x] 任务页：进度、失败原因、暂停/继续/停止/重试。
- [x] 结果页：视频预览、清单和定位输出文件入口。
- [x] 每个成片下方展示关键帧概览，可展开全部；悬停/聚焦放大，点击定位播放器。
- [x] 关键帧按需生成与缓存；覆盖全片、保留时间戳，避免任务轮询重复加载预览。
- [x] 每条成功成片提供“通过审核”，完整复制到可设置的审核目录，按日期编号子目录归档。
- [x] 审核状态和路径持久化，复制完整性校验、防重复、防同名覆盖，失败不标为通过；原导出保留。
- [x] 贴图库：导入静态PNG/JPG/WEBP，保留透明通道，受管存储和内容去重。
- [x] 位置模板：拖动定位、尺寸、不透明度、出现时间，多层保存复用；可选择成片作为参考画面。
- [x] 生成时可选贴图模板，默认不添加；批次保存不可变的模板快照。
- [x] 审核时套用模板生成独立新版本，音轨直接复制、原成片/审核状态保留，再单独审核新版本。
- [x] 从音乐文件夹识别风格，按“风格_编号.mp4”命名新成片；跨风格合并标签，审核/贴图版本保留风格，旧文件不改名。
- [x] 内置定时任务：一次/每日/每周、目标数量、素材检查间隔、独立目录与剪辑参数快照。
- [x] 素材不足先生成部分；发现新素材后补足，成功数量累计，当前轮次跨检查去重。
- [x] 新轮次替换旧定时目标，取消未完成队列/编码，保留成片，不影响手动批次。
- [x] 持久化计划和历史进度、重启恢复、错过时间只启动最新一轮；不自动审核或发布。
- [x] 可设源视频最低时长门槛（分钟，严格大于，0不限制）；手动单源/多源与定时快照一致生效。
- [x] 本地服务仅监听本机；文件接口按已扫描素材标识和批次结果白名单提供访问。
- [x] 提供双击启动入口，打开浏览器并检测重复启动。
- [x] 确认刷新网页不会结束后台，提供明确的退出后台方式。
- [x] 网页生成方案、开始、暂停、重试流程已实测，历史批次独立保存。

## M5：交付验收

- [x] 写使用说明：准备素材、两种模式、完整歌曲与时长关系、续跑和故障处理。
- [x] 说明默认项：720p/30fps、原音静音、歌曲每条至多一次、允许跨成片复用。
- [x] 记录当前机器实际速度与空间占用，不使用未经测试的性能承诺。
- [x] 整理验收记录：样片、50 条结果、去重统计、中断恢复结果。
- [x] 确认所有原始素材未被覆盖，生成文件与素材分开存放（13个素材指纹全部匹配）。

## 后续候选（不阻塞第一版）

- [ ] Mac 桌面安装包和运行依赖打包。
- [ ] 跨批次去重与历史素材使用统计。
- [ ] 实测后开放有限并发和更多导出规格。
- [ ] 自动精彩片段筛选、音乐开头评估、可选 Agent 接口。

下一步：使用720p样片收集用户听感与审美反馈；桌面安装包和模型能力按后续需求推进。
